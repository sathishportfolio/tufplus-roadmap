package Level3.C13_BinaryTrees.P10_PrintAllNodesAtADistanceOfKInBt.Revision1;

import java.util.Arrays;
/**
 * Category: Binary Trees
 * Subcategory: FAQs
 * Problem: Print all nodes at a distance of K in BT
 */
public class A2_Better_PrintAllNodesAtADistanceOfKInBt {

    // Solution : Better Approach - Print all nodes at a distance of K in BT
    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        solution(arr);
    }

    public static void solution(int[] arr) {
        System.out.println("\n Print all nodes at a distance of K in BT - Better Approach : " + Arrays.toString(arr));
    }
}
